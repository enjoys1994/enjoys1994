import java.time.Duration;
import java.time.ZoneId;
import java.time.ZonedDateTime;

public class TimeZoneDifferenceExample {
    public static void main(String[] args) {
//        ZoneId currentTimeZone = ZoneId.of("America/New_York"); // 指定巴黎时区
//        // 获取当前时间
//        ZonedDateTime currentTime = ZonedDateTime.now();
//
//        // 指定另一个时区
//        ZoneId anotherTimeZone = ZoneId.of("Europe/Paris"); // 指定巴黎时区
//
//        // 获取另一个时区的当前时间
//        ZonedDateTime anotherZoneTime = currentTime.withZoneSameInstant(anotherTimeZone);
//
//        // 计算两个时区之间的时间差
//        Duration timeDifference = Duration.between(currentTime, anotherZoneTime);
//
//        // 打印时间差
//        System.out.println("时间差：" + timeDifference);
        Boolean a = true;
        Boolean b= null;
        System.out.println(a.equals(b));;
    }
}
